# Wedding Planner Environment Configuration

# Server Port
PORT=3000

# JWT Secret Key (IMPORTANT: Change this in production!)
# Generate a secure random string for production use
JWT_SECRET=wedding-planner-secret-key-change-in-production

# Database Path (optional, defaults to ./wedding_planner.db)
# DB_PATH=./wedding_planner.db
